function fileExplorerController($scope, $route, $routeParams, $location, $http, blockUI, Upload) {
    $scope.usingFlash = FileAPI && FileAPI.upload != null;

    $scope.initFunc = function () {

    };
}